import { UniquementNombreDirective } from './uniquement-nombre.directive';

describe('UniquementNombreDirective', () => {
  it('should create an instance', () => {
    const directive = new UniquementNombreDirective();
    expect(directive).toBeTruthy();
  });
});
